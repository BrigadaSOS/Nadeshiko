meta {
  name: Delete series
  type: http
  seq: 25
}

delete {
  url: {{baseUrl}}/v1/media/series/:id
  body: none
  auth: bearer
}

params:query {
  id:
}

auth:bearer {
  token: {{apiKey}}
}

settings {
  encodeUrl: true
  timeout: 0
}
