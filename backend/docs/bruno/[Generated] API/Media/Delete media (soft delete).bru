meta {
  name: Delete media (soft delete)
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/v1/media/:id
  body: none
  auth: apikey
}

headers {
  X-API-Key: {{apiKey}}
}

params:query {
  id:
}

auth:apikey {
  key: X-API-Key
  value: {{apiKey}}
  placement: header
}

settings {
  encodeUrl: true
  timeout: 0
}
