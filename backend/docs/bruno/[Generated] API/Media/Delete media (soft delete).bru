meta {
  name: Delete media (soft delete)
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/v1/media/:id
  body: none
  auth: bearer
}

params:query {
  id:
}

auth:bearer {
  token: {{apiKey}}
}

settings {
  encodeUrl: true
  timeout: 0
}
