meta {
  name: Check if the user has permission
  type: http
  seq: 87
}

post {
  url: {{baseUrl}}/v1/auth/admin/has-permission
  body: none
  auth: none
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
