meta {
  name: stopImpersonating
  type: http
  seq: 79
}

post {
  url: {{baseUrl}}/v1/auth/admin/stop-impersonating
  body: none
  auth: none
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
