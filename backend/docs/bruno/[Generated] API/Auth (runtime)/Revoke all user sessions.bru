meta {
  name: Revoke all user sessions
  type: http
  seq: 99
}

post {
  url: {{baseUrl}}/v1/auth/admin/revoke-user-sessions
  body: none
  auth: none
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
