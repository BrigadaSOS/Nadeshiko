meta {
  name: changeEmail
  type: http
  seq: 85
}

post {
  url: {{baseUrl}}/v1/auth/change-email
  body: none
  auth: none
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
