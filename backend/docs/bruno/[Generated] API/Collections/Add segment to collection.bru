meta {
  name: Add segment to collection
  type: http
  seq: 58
}

post {
  url: {{baseUrl}}/v1/collections/:id/segments?id=V1StGXR8_Z5d
  body: json
  auth: none
}

params:query {
  id: V1StGXR8_Z5d
}

body:json {
  {
    "segmentId": "abc123def456",
    "note": ""
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
