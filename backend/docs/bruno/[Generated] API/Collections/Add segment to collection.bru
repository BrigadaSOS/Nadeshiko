meta {
  name: Add segment to collection
  type: http
  seq: 51
}

post {
  url: {{baseUrl}}/v1/collections/:id/segments
  body: json
  auth: none
}

params:query {
  id:
}

body:json {
  {
    "segmentUuid": "",
    "note": ""
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
