meta {
  name: Get collection details
  type: http
  seq: 52
}

get {
  url: {{baseUrl}}/v1/collections/:id?take=20
  body: none
  auth: none
}

params:query {
  id:
  cursor:
  take: 20
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
