meta {
  name: Delete collection
  type: http
  seq: 57
}

delete {
  url: {{baseUrl}}/v1/collections/:id?id=V1StGXR8_Z5d
  body: none
  auth: none
}

params:query {
  id: V1StGXR8_Z5d
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
