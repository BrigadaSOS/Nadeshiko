meta {
  name: Delete list
  type: http
  seq: 29
}

delete {
  url: {{baseUrl}}/v1/lists/:id
  body: none
  auth: bearer
}

params:query {
  id:
}

auth:bearer {
  token: {{apiKey}}
}

settings {
  encodeUrl: true
  timeout: 0
}
