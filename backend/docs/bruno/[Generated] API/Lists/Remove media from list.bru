meta {
  name: Remove media from list
  type: http
  seq: 35
}

delete {
  url: {{baseUrl}}/v1/lists/:id/items/:mediaId
  body: none
  auth: apikey
}

headers {
  X-API-Key: {{apiKey}}
}

params:query {
  id:
  mediaId:
}

auth:apikey {
  key: X-API-Key
  value: {{apiKey}}
  placement: header
}

settings {
  encodeUrl: true
  timeout: 0
}
