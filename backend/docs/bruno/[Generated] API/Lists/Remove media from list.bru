meta {
  name: Remove media from list
  type: http
  seq: 33
}

delete {
  url: {{baseUrl}}/v1/lists/:id/items/:mediaId
  body: none
  auth: bearer
}

params:query {
  id:
  mediaId:
}

auth:bearer {
  token: {{apiKey}}
}

settings {
  encodeUrl: true
  timeout: 0
}
