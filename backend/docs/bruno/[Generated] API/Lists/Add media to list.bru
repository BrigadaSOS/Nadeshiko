meta {
  name: Add media to list
  type: http
  seq: 31
}

post {
  url: {{baseUrl}}/v1/lists/:id/items
  body: json
  auth: bearer
}

params:query {
  id:
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "mediaId": 0,
    "position": 0
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
