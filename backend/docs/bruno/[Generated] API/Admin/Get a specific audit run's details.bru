meta {
  name: Get a specific audit run's details
  type: http
  seq: 76
}

get {
  url: {{baseUrl}}/v1/admin/media/audits/runs/:id
  body: none
  auth: none
}

params:query {
  id:
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
