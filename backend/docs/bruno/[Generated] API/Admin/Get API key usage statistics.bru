meta {
  name: Get API key usage statistics
  type: http
  seq: 65
}

get {
  url: {{baseUrl}}/v1/admin/dashboard/api-keys
  body: none
  auth: none
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
