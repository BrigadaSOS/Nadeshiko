meta {
  name: Retry failed jobs from a queue
  type: http
  seq: 62
}

post {
  url: {{baseUrl}}/v1/admin/queues/:queueName/retry
  body: none
  auth: bearer
}

params:query {
  queueName:
}

auth:bearer {
  token: {{apiKey}}
}

settings {
  encodeUrl: true
  timeout: 0
}
