meta {
  name: Remove from allowlist
  type: http
  seq: 71
}

delete {
  url: {{baseUrl}}/v1/admin/review/allowlist/:id
  body: none
  auth: bearer
}

params:query {
  id:
}

auth:bearer {
  token: {{apiKey}}
}

settings {
  encodeUrl: true
  timeout: 0
}
