meta {
  name: Get dashboard overview statistics
  type: http
  seq: 61
}

get {
  url: {{baseUrl}}/v1/admin/dashboard/overview
  body: none
  auth: none
}

params:query {
  days:
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
