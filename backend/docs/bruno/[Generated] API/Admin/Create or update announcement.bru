meta {
  name: Create or update announcement
  type: http
  seq: 83
}

put {
  url: {{baseUrl}}/v1/admin/announcement
  body: json
  auth: none
}

body:json {
  {
    "message": "",
    "type": "",
    "active": false
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
