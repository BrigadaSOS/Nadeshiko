meta {
  name: Update report
  type: http
  seq: 72
}

patch {
  url: {{baseUrl}}/v1/admin/reports/:reportId
  body: json
  auth: bearer
}

params:query {
  reportId:
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "status": "OPEN",
    "adminNotes": "Confirmed wrong translation, will fix"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
