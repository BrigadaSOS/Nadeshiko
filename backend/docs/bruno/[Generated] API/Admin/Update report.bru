meta {
  name: Update report
  type: http
  seq: 79
}

patch {
  url: {{baseUrl}}/v1/admin/reports/:id
  body: json
  auth: none
}

params:query {
  id:
}

body:json {
  {
    "status": "ACCEPTED",
    "adminNotes": "Confirmed wrong translation, will fix"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
