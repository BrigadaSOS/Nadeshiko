meta {
  name: Forget one title from the reader's tally
  type: http
  seq: 43
}

delete {
  url: {{baseUrl}}/v1/user/familiar-media/:mediaPublicId?mediaPublicId=V1StGXR8_Z5d
  body: none
  auth: none
}

params:query {
  mediaPublicId: V1StGXR8_Z5d
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
