meta {
  name: Delete activity record
  type: http
  seq: 35
}

delete {
  url: {{baseUrl}}/v1/user/activity/:activityId
  body: none
  auth: none
}

params:query {
  activityId:
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
