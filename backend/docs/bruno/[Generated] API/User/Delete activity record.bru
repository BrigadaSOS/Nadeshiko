meta {
  name: Delete activity record
  type: http
  seq: 45
}

delete {
  url: {{baseUrl}}/v1/user/activity/:id
  body: none
  auth: none
}

params:query {
  id:
}

settings {
  encodeUrl: true
  timeout: 0
}

script:pre-request {
  const sessionToken = bru.getEnvVar("sessionToken");
  if (sessionToken) {
    req.setHeader("Cookie", "nadeshiko.session_token=" + sessionToken);
  }
}
