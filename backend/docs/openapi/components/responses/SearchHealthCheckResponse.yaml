type: object
properties:
  results:
    type: array
    items:
      $ref: "../schemas/SearchResult.yaml"
  pagination:
    $ref: "../schemas/PaginationInfo.yaml"
  cursor:
    type: array
    items:
      type: number
    description: Cursor for pagination
