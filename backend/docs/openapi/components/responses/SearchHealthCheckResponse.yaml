type: object
properties:
  sentences:
    type: array
    items:
      $ref: "../schemas/Sentence.yaml"
  queryStats:
    $ref: "../schemas/QueryStats.yaml"
  cursor:
    type: array
    items:
      type: number
    description: Cursor for pagination
