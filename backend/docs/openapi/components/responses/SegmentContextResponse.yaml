type: object
required:
  - segments
properties:
  segments:
    type: array
    items:
      $ref: "../schemas/segment/Segment.yaml"
  includes:
    type: object
    description: Optional related resources requested via `include[]`
    properties:
      media:
        type: object
        additionalProperties:
          $ref: "../schemas/Media.yaml"
        description: Media objects keyed by publicId. Present only when `include[]=media` is requested.
