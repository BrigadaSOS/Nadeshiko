type: object
required:
  - stats
  - results
  - pagination
properties:
  stats:
    $ref: "../schemas/MediaBrowseStats.yaml"
  results:
    type: array
    items:
      $ref: "../schemas/MediaSummary.yaml"
  pagination:
    $ref: "../schemas/CursorPagination.yaml"
