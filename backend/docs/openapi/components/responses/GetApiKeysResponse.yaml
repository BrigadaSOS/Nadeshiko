type: object
description: List of API keys response
properties:
  keys:
    type: array
    items:
      $ref: "../schemas/ApiKey.yaml"
    description: List of API keys
  quota:
    $ref: "../schemas/QuotaInfo.yaml"
required:
  - keys
  - quota
