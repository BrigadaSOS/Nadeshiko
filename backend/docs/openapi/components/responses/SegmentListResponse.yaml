type: object
required:
  - segments
  - pagination
properties:
  segments:
    type: array
    items:
      $ref: "../schemas/segment/Segment.yaml"
  pagination:
    $ref: "../schemas/CursorPagination.yaml"
