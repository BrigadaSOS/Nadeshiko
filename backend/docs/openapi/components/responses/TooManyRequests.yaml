description: Too Many Requests. The response body indicates whether the request was rejected due to per-minute rate limiting or monthly quota exhaustion.
content:
  application/json:
    schema:
      $ref: "../schemas/Error429.yaml"
    examples:
      RateLimitExceeded:
        $ref: "../examples/RateLimitExceeded.yaml"
      QuotaExceeded:
        $ref: "../examples/QuotaExceeded.yaml"
