type: object
description: Create API key request
properties:
  name:
    type: string
    description: Name for the API key
  permissions:
    type: array
    items:
      type: string
    description: List of permissions for the API key. Defaults to READ_MEDIA if not provided.
required:
  - name
