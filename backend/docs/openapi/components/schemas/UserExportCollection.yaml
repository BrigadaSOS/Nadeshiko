allOf:
  - $ref: "./collection/Collection.yaml"
  - type: object
    required:
      - segmentIds
    properties:
      segmentIds:
        type: array
        description: Segment IDs in saved order
        items:
          type: integer
