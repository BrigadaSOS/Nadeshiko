type: object
description: Activity counts for a single day, broken down by type. Only types with at least 1 event are present.
properties:
  SEARCH:
    type: integer
    minimum: 0
  SEGMENT_PLAY:
    type: integer
    minimum: 0
  ANKI_EXPORT:
    type: integer
    minimum: 0
  SHARE:
    type: integer
    minimum: 0
