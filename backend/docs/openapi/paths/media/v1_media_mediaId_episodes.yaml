get:
  operationId: listEpisodes
  tags:
    - Media
  summary: List episodes for a media
  description: |
    Returns a paginated list of episodes for a specific media.

    **Permissions:** `READ_MEDIA`

  security:
    - ApiKey: [READ_MEDIA]
    - SessionCookie: []
  parameters:
    - name: mediaId
      in: path
      required: true
      schema:
        type: string
      description: Public ID of the media
      example: "V1StGXR8_Z5d"
    - name: take
      in: query
      required: false
      schema:
        type: integer
        default: 50
        minimum: 1
        maximum: 100
      description: Maximum number of episodes to return
      example: 50
    - name: cursor
      in: query
      required: false
      schema:
        type: string
      description: Opaque pagination cursor token
      example: 10
  responses:
    "200":
      $ref: "../../components/responses/EpisodeListResponse.yaml"
    "400":
      $ref: "../../components/responses/BadRequest.yaml"
    "401":
      $ref: "../../components/responses/UnauthorizedApiKey.yaml"
    "403":
      $ref: "../../components/responses/Forbidden.yaml"
    "404":
      $ref: "../../components/responses/NotFound.yaml"
    "429":
      $ref: "../../components/responses/TooManyRequests.yaml"
    "500":
      $ref: "../../components/responses/InternalServerError.yaml"

post:
  operationId: createEpisode
  x-internal: true
  tags:
    - Media
  summary: Create new episode
  description: |
    Creates a new episode for a specific media.

    **Permissions:** `ADD_MEDIA`

  security:
    - ApiKey: [ADD_MEDIA]
    - SessionCookie: []
  parameters:
    - name: mediaId
      in: path
      required: true
      schema:
        type: string
      description: Public ID of the media
      example: "V1StGXR8_Z5d"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/EpisodeCreateRequest.yaml"
  responses:
    "201":
      $ref: "../../components/responses/EpisodeResponse.yaml"
    "400":
      $ref: "../../components/responses/BadRequest.yaml"
    "401":
      $ref: "../../components/responses/UnauthorizedApiKey.yaml"
    "403":
      $ref: "../../components/responses/Forbidden.yaml"
    "404":
      $ref: "../../components/responses/NotFound.yaml"
    "409":
      $ref: "../../components/responses/Conflict.yaml"
    "429":
      $ref: "../../components/responses/TooManyRequests.yaml"
    "500":
      $ref: "../../components/responses/InternalServerError.yaml"
