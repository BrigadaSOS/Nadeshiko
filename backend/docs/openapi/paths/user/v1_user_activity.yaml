get:
  tags:
    - User
  x-internal: true
  operationId: listUserActivity
  summary: List user activity
  description: |
    Returns the authenticated user's activity history with cursor-based pagination.

  security:
    - SessionCookie: []
  parameters:
    - name: cursor
      in: query
      schema:
        type: string
      description: Opaque cursor token for keyset pagination
    - name: take
      in: query
      schema:
        type: integer
        default: 20
        maximum: 100
      description: Number of results per page
    - name: activityType
      in: query
      schema:
        $ref: "../../components/schemas/ActivityType.yaml"
      description: Filter by activity type
    - name: date
      in: query
      schema:
        type: string
        format: date
      description: Filter activities to a specific date (YYYY-MM-DD)
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            required:
              - activities
              - pagination
            properties:
              activities:
                type: array
                items:
                  $ref: "../../components/schemas/UserActivity.yaml"
              pagination:
                $ref: "../../components/schemas/OpaqueCursorPagination.yaml"
    "401":
      $ref: "../../components/responses/Unauthorized.yaml"
    "500":
      $ref: "../../components/responses/InternalServerError.yaml"

post:
  tags:
    - User
  x-internal: true
  operationId: trackUserActivity
  summary: Track user activity
  description: |
    Records a user activity event (e.g. segment play). Fire-and-forget — always returns 204.

  security:
    - SessionCookie: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required:
            - activityType
          properties:
            activityType:
              type: string
              enum:
                - SEARCH
                - SEGMENT_PLAY
                - SHARE
                - ANKI_EXPORT
            segmentId:
              type: string
            mediaId:
              type: integer
            mediaName:
              type: string
            japaneseText:
              type: string
            searchQuery:
              type: string
  responses:
    "204":
      description: No Content
    "400":
      $ref: "../../components/responses/BadRequest.yaml"
    "401":
      $ref: "../../components/responses/Unauthorized.yaml"
    "500":
      $ref: "../../components/responses/InternalServerError.yaml"

delete:
  tags:
    - User
  x-internal: true
  operationId: deleteUserActivity
  summary: Clear user activity
  description: |
    Deletes the authenticated user's activity history. Optionally filter by activity type.

  security:
    - SessionCookie: []
  parameters:
    - name: activityType
      in: query
      schema:
        $ref: "../../components/schemas/ActivityType.yaml"
      description: Only clear activities of this type
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            required:
              - deletedCount
            properties:
              deletedCount:
                type: integer
    "401":
      $ref: "../../components/responses/Unauthorized.yaml"
    "500":
      $ref: "../../components/responses/InternalServerError.yaml"
