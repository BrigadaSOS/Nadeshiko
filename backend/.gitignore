# Generated build and log files
node_modules
.env
!.env.test
prod
logs
.codex
*.log
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
docs/generated/index.html
# Written by `docs:bundle:public` for consumers that regenerate it first (the
# CI oasdiff job, the release artifact). Committing it only creates a second
# copy of the public contract to keep in sync with the one `docs:build`
# publishes under frontend/public/docs/api -- and the stale one is the one git
# presents as authoritative.
docs/generated/openapi-public.yaml
# Built by `generate:auth-spec` from the main bundle plus better-auth's schema.
docs/generated/openapi-sdk.yaml
