{
  "name": "@brigadasos/nadeshiko-sdk",
  "version": "2.3.0",
  "private": true,
  "type": "module",
  "//": "Workspace consumers import TypeScript source from generated/internal. The npm package that external users install is built and published by the nadeshiko-sdk-ts repository.",
  "exports": {
    ".": "./generated/internal/index.ts"
  },
  "types": "./generated/internal/index.ts",
  "scripts": {
    "codegen": "node --import tsx scripts/generateInternal.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf generated"
  },
  "devDependencies": {
    "@hey-api/openapi-ts": "^0.90.9",
    "typescript": "^6.0.3",
    "vitest": "^3.2.4",
    "yaml": "^2.8.2"
  },
  "description": "Typed Nadeshiko API client used by the frontend and the Discord bot."
}
