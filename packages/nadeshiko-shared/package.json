{
  "name": "@brigadasos/nadeshiko-shared",
  "version": "2.4.12",
  "private": true,
  "type": "module",
  "//": "Code that two runtimes have to agree on, byte for byte. Workspace consumers import the TypeScript source directly -- there is no build step -- the way they already do for @brigadasos/nadeshiko-sdk. Nothing here may import a `node:` module or a runtime dependency: the frontend bundles it into the client as well.",
  "exports": {
    "./traffic": "./src/traffic.ts",
    "./logRedaction": "./src/logRedaction.ts"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "description": "Classification and redaction rules the API and the web frontend must answer identically."
}
