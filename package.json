{
  "name": "nadeshiko-root",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev:backend & bun run dev:frontend",
    "dev:backend": "bun run --cwd backend dev",
    "dev:frontend": "bun run --cwd frontend dev",
    "build": "bun run --cwd backend build && bun run --cwd frontend build",
    "check": "bun run lint && bun run typecheck && bun run build",
    "lint": "bun run --cwd backend lint && bun run --cwd frontend lint",
    "lint:fix": "bun run --cwd backend lint:fix && bun run --cwd frontend lint:fix",
    "format": "bun run --cwd backend format && bun run --cwd frontend format",
    "release:set-version": "bash -c 'bun run scripts/releaseVersion.ts set \"$1\" && bun run --cwd backend generate:all \"$1\" && bun run scripts/releaseVersion.ts check \"$1\"' --",
    "release:check-version": "bun run scripts/releaseVersion.ts check"
  }
}
